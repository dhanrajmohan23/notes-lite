import { styled } from "@mui/material";
import { Box } from "@mui/material";

export namespace S {
  export const MainContainer = styled(Box)(() => ({
    background: "lavender",
    width: "100%",
  }));
}
